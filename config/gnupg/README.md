# GPG
[GPG](https://www.gnupg.org/) is an implementation of the OpenPGP standard, used for cryptography.

This contain only the configuration for the pin entry program.

## Configuration location
The configuration file is located at `~/.gnupg/`.

## File
- `gpg-agent.conf`: the configuration file for gnupg
